# Q-Vida Frontend

```
main        ← producción, solo merge desde develop
develop     ← rama de trabajo principal
feature/*   ← features nuevas, se mergean a develop (opcional)
hotfix/*    ← fixes urgentes en producción, merge a main y develop
```